# STEP3：登録フォーム → Firestore に保存（写真なし・テキストだけ）

## ゴール
「登録する」を押したら、入力内容が Firestore の `profiles` に1件保存される。
（※画面にカードが出るのは STEP4。保存できたかは Firebase 画面で確認する）

## 作業項目
- [x] 1. functions.js を「モジュール版」に切り替え（index.html の読み込みタグを type="module" に）
- [x] 2. Firestore の道具を import（collection / addDoc / serverTimestamp）
- [x] 3. タグボタンを TAGS 配列から自動生成（今の仮表示を本物に差し替え）
- [x] 4. タグの単一選択（1つだけ選べる・選ぶと色が変わる・選んだタグを覚える）
- [x] 5. saveProfile()：登録ボタンで実行
    - [x] 名前が空ならエラー（名前は必須・他は任意）
    - [x] 入力をまとめてオブジェクトにする（name/affiliation/comment/tag/sns/url/photoUrl=""/createdAt）
    - [x] addDoc で profiles に保存（createdAt は serverTimestamp）
    - [x] 成功したら「登録しました！」→ フォームを空に → 一覧へ戻る
    - [x] 失敗したらエラー内容を知らせる

## 検証
- [x] Live Server で開く → フォーム入力 → 登録 → Firebase の profiles に新しいデータが増える（確認済み）

## レビュー（完了後に記入）
- functions.js をモジュール版に切り替え、タグ自動生成・単一選択・保存処理を実装。
- フォーム入力 → 「登録しました！」→ Firestore の profiles に正しく保存されることを実機（Live Server）で確認済み。
- 写真は未対応（photoUrl: ""）。STEP5 で対応予定。
- 次は STEP4：保存したカードを画面にリアルタイム表示。


# STEP4：保存したカードを画面にリアルタイム表示

## ゴール
profiles に保存されたカードが、全員の画面に自動で並ぶ（新着順・タグ別セクション）。

## 作業項目
- [x] 1. index.html の仮カードを削除（JSが並べる空の土台に）
- [x] 2. onSnapshot を import に追加
- [x] 3. renderSections()：TAGS からタグ別セクションを自動生成（card-row に data-tag の印）
- [x] 4. createCard()：プロフィール1人分のカードを作る（.text() で安全に表示）
- [x] 5. renderCards()：全カードを並べ直す（タグに合うセクションに振り分け）
- [x] 6. listenProfiles()：onSnapshot で profiles を見張り、変化のたびに新着順で再描画
- [x] 7. 起動時に renderSections() と listenProfiles() を呼ぶ
- [x] タグは必ず1つ選択（初期値＝先頭）にして迷子カードを防止

## 検証
- [x] Live Server で開く → 既存データがカードで表示される（確認済み）
- [x] 新しく登録 → 自分の画面に即カードが増える（確認済み）
- [ ] 別タブをもう1つ開く → 片方で登録すると両方に同時に増える（リアルタイム）

## レビュー（完了後に記入）
- 「人がいるタグのセクションだけ表示」に変更（.filter で絞り込み）。空のタグ見出しは出ない。


# STEP6：詳細モーダル

## ゴール
カードをタップすると、その人の詳細が拡大表示される（写真大きめ・一言フル・SNS/HPリンク）。

## 作業項目
- [x] 1. createCard にタップ時の処理を追加（openDetail を呼ぶ）
- [x] 2. openDetail()：モーダルの中身を組み立てて表示（名前・所属・一言・リンク）
- [x] 3. addLink()：URL があるリンクだけボタンを作る（空は出さない）
- [x] 4. closeDetail()：閉じる。×ボタンと背景タップの両方で閉じられる
- [x] 5. モーダル中身の見た目（CSS）を追加

## 検証
- [x] カードをタップ → 詳細モーダルが開く（確認済み）
- [x] 一言がフルで表示される／入力したSNS・HPリンクがボタンで出る・リンク先に飛べる
- [x] ×ボタン、背景タップのどちらでも閉じられる
- [x] 入力していないリンクは表示されない

## レビュー（完了後に記入）
- カードタップで詳細モーダル表示。リンクは入力があるものだけ表示。×／背景タップで閉じる。実機確認済み。


# STEP5：写真アップロード（無料版・Storageを使わず Firestore に保存）

## 背景
Firebase Storage は無料プラン(Spark)では使えず有料アップグレードが必要だったため、
写真は長辺320pxに縮小し、JPEGの文字データ(data URL)として Firestore に直接保存する方式に変更。

## 作業項目
- [x] 1. index.html から未使用の Storage 読み込み・初期化を削除
- [x] 2. handlePhotoSelect()：選んだ画像を canvas で長辺320pxに縮小→JPEG文字データ化→プレビュー
- [x] 3. applyPhoto()：写真があれば背景に表示、なければシルエット（カード・モーダル共通）
- [x] 4. saveProfile で photoUrl に写真データを保存／保存後 resetPhoto() でリセット
- [x] 5. photo-input の change を起動時にひも付け
- [x] 6. CSS：プレビュー中はカメラアイコンを隠す

## 検証
- [x] フォームで写真を選ぶ → 丸い枠にプレビューが出る（確認済み）
- [x] 登録 → カードと詳細モーダルに写真が表示される（確認済み）
- [ ] 写真なしで登録 → これまでどおりシルエット表示
- [ ] Firebase の profiles に photoUrl（長い文字データ）が入っている

## レビュー（完了後に記入）
- 写真は長辺320pxに縮小しJPEG文字データとして Firestore に保存。カード・モーダルに表示。実機確認済み。


# STEP7：「すべて表示」カード（しきい値5件）

## ゴール
1つのタグに6人以上いたら、最初の5枚＋末尾に「すべて表示（あと○人）」カードを出す。
押すとそのタグの全員を表示する。

## 作業項目
- [x] 1. CARDS_PER_SECTION = 5（設定で変更可能）
- [x] 2. expandedTags：展開済みタグを記憶／latestProfiles：再描画用に最新一覧を保持
- [x] 3. renderList：超過時は slice(0,5) で先頭5枚＋「すべて表示」カードを末尾に追加
- [x] 4. 「すべて表示」クリックで expandedTags[tag]=true → renderList で全員表示
- [x] 5. CSS：more-card（赤文字のカード型ボタン）

## 検証
- [ ] 同じタグで6人以上登録 → 5枚＋「すべて表示（あと○人）」が出る（← あなたに試してほしい）
- [ ] 「すべて表示」を押すと、そのタグの全員が表示される
- [ ] 5人以下のタグには「すべて表示」は出ない

## レビュー（完了後に記入）
-
